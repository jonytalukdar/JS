//  ternary operator
const name = 'joney';
const age = 21;
const result = age > 18 ? 'you are adult' : 'you are not adult';
console.log(result);

const active = true;
const userActive = active ? 'user are active' : 'user are not active';
console.log(userActive);

//  ternary function call shortcut
// active ? display() : displayNone();

// active && display();
const isActive = active && 5;
console.log(isActive);

//  string to number
const n = +'23';
console.log(n);

// number to string
const n1 = 12 + '';
console.log(n1);
