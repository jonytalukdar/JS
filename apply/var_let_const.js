// using var

var myName = 'Joney';
if (myName.length === 5) {
  myName = 'Joney Talukdar';
  console.log(myName);
} else {
  myName = 'Jony Talukdar';
  console.log(myName);
}

// using let

let herName = 'Jerry';
if (herName.length === 5) {
  let herName = 'Jerry Talukdar';
  console.log(herName);
} else {
  let herName = 'Jery Talukdar';
  console.log(herName);
}

// using const

const hisName = 'somethig';
if (hisName.length === 5) {
  hisName = 'Jerry Talukdar';
  console.log(hisName);
} else {
  hisName = 'Jery Talukdar';
  console.log(hisName);
}
